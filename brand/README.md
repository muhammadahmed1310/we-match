# Women Emerging brand source files

This folder keeps original brand files outside the Rails asset pipeline.

- `WE Logo EPS.eps` — source logo provided by WE.

Runtime images used by the app live in `app/assets/images/`:

- `we-logo.png` — dark blue mark on a transparent background for light surfaces
- `we-logo-reverse.png` — white reverse mark for dark surfaces
