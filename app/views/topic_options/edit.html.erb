<% content_for :title, "Edit option — WE Match" %>

<div class="page-header">
  <h1>Edit option</h1>
  <p class="muted"><%= @topic.name %></p>
</div>

<div class="card form-card">
  <%= render "form" %>
</div>
