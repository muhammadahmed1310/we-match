<div class="page-header">
  <h1>Submit Match Response</h1>
  <p class="muted"><%= @match_cycle.group.name %> — cycle #<%= @match_cycle.id %></p>
</div>

<div class="card form-card">
  <%= render "form", match_response: @match_response %>
</div>
